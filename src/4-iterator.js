/**
 * Fonction sur les iterators.
 * https://developer.mozilla.org/docs/Web/JavaScript/Reference/Global_Objects/Iterator
 */

/**
 * Rendre les propriétés d'un objet iterables.
 * Les valeurs émises par l'iterator doivent être des tuples, un tableau contenant le nom de la propriété de l'objet ainsi que sa valeur : ["propA", 1].
 * Pour rapel, un objet est itérable lorsque qu'il expose un fonction permétant de produire un nouvel iterator.
 * Cette fonction doit être disponible à la racine de l'objet, dans la propriété [Symbol.iterator].
 * 
 * S'aider de la fonction Object.keys : https://developer.mozilla.org/fr/docs/Web/JavaScript/Reference/Global_Objects/Object/keys
 */
export function toIterable(obj) {
    
}
